{
  "name": "backend",
  "version": "1.0.0",
  "description": "my first node.js greenfield project",
  "main": "index.js",
  "scripts": {
    "start": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rimraf dist && cd ../fsopen-react && npm run build && copyfiles -u 1 \"dist/**/*\" ../fsopen-node-express/dist",
    "deploy": "git push",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push",
    "logs:prod": "render logs"
  },
  "author": "Talha Mansoor",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "morgan": "^1.12.0"
  },
  "devDependencies": {
    "copyfiles": "^2.4.1",
    "rimraf": "^6.1.3"
  }
}
